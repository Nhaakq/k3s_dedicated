- name: Deploy Values charts
  ansible.builtin.template:
    src: templates/{{ item }}
    dest: /tmp/{{ item | basename | regex_replace('\.j2$', '') }}
    owner: "{{ kub_user }}"
    group: "{{ kub_group }}"
  loop:
    - sonarr_deployment.yaml.j2
    - sonarr_body_DownloadClients.j2
  tags:
    - sonarr_install

- name: Create a deployment
  kubernetes.core.k8s:
    apply: true
    state: present
    namespace: "{{ media_namespace }}"
    src: "/tmp/sonarr_deployment.yaml"
  tags:
    - sonarr_install

- name: Wait until the file {{ path_config }}/config/{{ app_name }}/config.xml exist
  ansible.builtin.wait_for:
    path: "{{ path_config }}/config/{{ app_name }}/config.xml"
    delay: 15
  tags:
    - sonarr_install

- name: Find {{ app_name }} Api Key
  ansible.builtin.slurp:
    src: "{{ path_config }}/config/{{ app_name }}/config.xml"
  register: api_key
  tags:
    - api

- name: Setting Fact Api Key
  ansible.builtin.set_fact:
    api_key_sonarr: "{{ api_key['content'] | b64decode | regex_findall('<ApiKey>([^<]*)</ApiKey>') | first }}"
  tags:
    - api

- name: Check {{ app_name }} configuration for Torrent client
  ansible.builtin.uri:
    url: https://{{ web_name }}.{{ domain_name }}/api/v3/downloadclient?apikey={{ api_key_sonarr }}
    method: GET
    return_content: true
    validate_certs: false
  register: is_client_exist
  tags:
    - api

- name: Get body file
  ansible.builtin.slurp:
    src: "/tmp/sonarr_body_DownloadClients"
  register: body_json
  when: is_client_exist.json|length < 1
  tags:
    - api

- name: Create Transmission Configuration
  ansible.builtin.uri:
    url: https://{{ web_name }}.{{ domain_name }}/api/v3/downloadclient?apikey={{ api_key_sonarr }}
    method: POST
    body_format: json
    status_code: [200, 202, 201]
    return_content: true
    validate_certs: false
    headers:
      Content-Type: application/json
      User-Agent: PostmanRuntime/7.29.2
      Accept-Encoding: gzip, deflate, br
    body: "{{ body_json['content'] | b64decode }}"
  when: is_client_exist.json|length < 1
  tags:
    - api