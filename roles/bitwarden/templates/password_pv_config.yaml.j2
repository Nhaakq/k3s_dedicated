kind: PersistentVolume
apiVersion: v1
metadata:
   name: {{ name_pv_config_password }}
   labels:
      type: local
spec:
   storageClassName: config
   capacity:
      storage: {{ capacity_pv_config_password }}Gi
   accessModes:
      - ReadWriteMany
   hostPath:
      path: "{{ path_config_password }}"
